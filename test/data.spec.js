// Ejecuta tu test
const {assert}=require("chai");
const {InputNotEmpty} = require( from ../src/js/index.js);
describe("InputNotEmpty()", ()=>{
  it("deberia ser una funcion", ()=>{
  assert.equal(typeof funciones.InputNotEmpty, "function")
});
it("deberia return true en caso de el input no estè vacio")
})
